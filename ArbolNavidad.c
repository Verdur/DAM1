#include <stdio.h>

main(){
	int copa = {0}, base = {0};
	printf ("Introduce la copa:");
	scanf ("%d", &copa);
	fflush (stdin);
	printf ("\nIntroduce la base:");
	scanf ("%d", &base);
	fflush (stdin);
	
	
	
}
