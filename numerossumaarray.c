#include <stdio.h>
#define TAM 10

void peticion (int * input);
void llenar (const int array[]):
main(){
	int buscar = {0}, array [TAM] = {0};
	peticion(&buscar);
	llenar (array);
	do{
		do{
		}while(sum!=buscar && f<TAM);
	}while(sum!=buscar && i<TAM);
	system ("pause");
	return 0;
}

void peticion (int * input){
	scanf ("%d",&*input);
	fflush (stdin);
	return;
}
void llenar (const int array[]){
	int i = {0};
	for (i = 0; i<TAM; ++i){
		scanf ("%d",&array[i]);
		fflush (stdin);
	}
	return;
}
